# Example backend configuration for local development
# Copy this to backend.hcl and update the values for your environment
# 
# Usage:
#   terraform init -backend-config=backend.hcl
#
# Or supply each value individually:
#   terraform init \
#     -backend-config="bucket=quest-terraform-state-cbell-0001" \
#     -backend-config="key=quest/terraform.tfstate" \
#     -backend-config="region=us-east-1" \
#     -backend-config="encrypt=true"

bucket         = "quest-terraform-state-cbell-0001"
key            = "quest/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
# If you want locking, set `dynamodb_table` manually and create the table.
# dynamodb_table = "quest-terraform-locks"
